# 修复 Vercel 总是部署旧提交的问题

## 🔍 问题

Vercel **总是部署旧提交 `bb5869b`**，而不是最新提交 `cb601fd`。

**原因**：
- Vercel 可能缓存了旧的部署配置
- 或者 Git 连接指向了错误的提交
- 或者项目设置中指定了特定提交

## ✅ 解决方案

### 方法 1：断开并重新连接 Git（最有效）

1. **进入 Vercel Dashboard**：
   - 访问 https://vercel.com
   - 进入项目 `vote1`

2. **断开 Git 连接**：
   - Settings → Git
   - 找到 "Connected Git Repository"
   - 点击 **"Disconnect"** 或 **"..."** → **"Disconnect"**
   - 确认断开

3. **重新连接 Git**：
   - 点击 **"Connect Git Repository"**
   - 搜索并选择 `water4699/vote1`
   - 选择分支：`main`
   - 点击 **"Connect"**

4. **配置项目设置**：
   - Root Directory: `ui`
   - Framework Preset: `Next.js`
   - Build Command: **留空**
   - Output Directory: **留空**
   - Install Command: **留空**

5. **部署**：
   - 重新连接后，Vercel 会自动触发部署
   - 这次应该使用最新提交

### 方法 2：在部署时手动选择提交

1. **进入 Deployments 页面**：
   - 点击 "Deployments" 标签

2. **创建新部署**：
   - 点击 **"Redeploy"** 按钮
   - 在弹窗中，**不要**直接点击 "Redeploy"
   - 查找 **"Select a different commit"** 或类似选项
   - 选择最新提交 `cb601fd` 或 `c2b291c`
   - 然后点击 "Redeploy"

3. **验证提交**：
   - 部署开始后，检查构建日志
   - 确认提交哈希是 `cb601fd` 或更新的，不是 `bb5869b`

### 方法 3：清除所有部署并重新开始

1. **删除项目并重新创建**（最后手段）：
   - Settings → General
   - 滚动到底部
   - 点击 **"Delete Project"**
   - 确认删除

2. **重新创建项目**：
   - 点击 "Add New..." → "Project"
   - 导入 `water4699/vote1`
   - 配置设置：
     - Root Directory: `ui`
     - Framework Preset: `Next.js`
     - 其他设置留空
   - 点击 "Deploy"

## 📋 验证最新提交

### 检查 GitHub

访问 https://github.com/water4699/vote1，确认：
- 最新提交是 `cb601fd`
- `ui/package.json` 内容是最小化的：
  ```json
  {
    "name": "root",
    "version": "0.4.0",
    "private": true
  }
  ```

### 检查本地

```bash
git log --oneline -3
# 应该看到：
# cb601fd Add manual deployment guide
# c2b291c Trigger Vercel deployment with latest fixes
# 4c7954a Add guide for forcing Vercel to redeploy latest commit

git show HEAD:ui/package.json
# 应该只包含基本字段，没有 scripts 或 engines
```

## ⚠️ 重要提示

**确保 Vercel 部署的是最新提交 `cb601fd`，而不是 `bb5869b`。**

如果重新连接 Git 后仍然部署旧提交：
1. 检查 GitHub 仓库是否真的包含最新提交
2. 尝试方法 2，手动选择提交
3. 如果都不行，考虑方法 3，重新创建项目

## 🎯 推荐操作顺序

1. **首先尝试方法 1**（断开并重新连接 Git）
   - 这是最可能解决问题的方法
   - 重新连接后会自动使用最新提交

2. **如果方法 1 不行，尝试方法 2**（手动选择提交）
   - 在部署时明确选择最新提交

3. **最后尝试方法 3**（重新创建项目）
   - 如果前两种方法都不行

## ✅ 部署成功后验证

部署成功后，检查构建日志应该看到：

1. **正确的提交**：
   ```
   Cloning github.com/water4699/vote1 (Branch: main, Commit: cb601fd)
   ```
   或更新的提交哈希

2. **没有 preinstall 错误**：
   - 不应该看到 `> root@0.4.0 preinstall`
   - 不应该看到 `> pnpm sdk:build`
   - 不应该看到 pnpm 版本错误

3. **使用 vercel.json 的 installCommand**：
   ```
   Running "cd packages/fhevm-sdk && npm install --legacy-peer-deps && npm run build && cd ../nextjs && npm install --legacy-peer-deps"
   ```

4. **构建成功**：
   - SDK 构建成功
   - Next.js 应用构建成功
   - 部署完成

