{
  "installCommand": "cd ../fhevm-sdk && npm install --legacy-peer-deps && npm run build && cd ../nextjs && npm install --legacy-peer-deps",
  "buildCommand": "node scripts/setup-sdk.js && npm run build"
}
