# Vercel 强制重新部署最新提交指南

## 🔍 问题诊断

Vercel 仍然在部署旧的提交 `bb5869b`（Initial commit），而不是最新的提交 `74cdf9e`。

**旧提交的问题**：
- `ui/package.json` 包含 `preinstall` 脚本
- `preinstall` 脚本运行 `pnpm sdk:build`
- 导致 pnpm 版本不匹配错误

**最新提交的修复**：
- `ui/package.json` 已最小化（只有基本字段）
- 移除了所有脚本和 pnpm 配置
- 应该能正常部署

## ✅ 解决方案：在 Vercel Dashboard 中强制重新部署

### 方法 1：从 Deployments 页面重新部署（推荐）

1. **访问 Vercel Dashboard**：
   - 登录 https://vercel.com
   - 进入项目 `vote1`

2. **进入 Deployments 页面**：
   - 点击顶部的 **"Deployments"** 标签
   - 你会看到所有部署记录

3. **找到最新的部署**：
   - 查看每个部署的提交哈希
   - 找到提交哈希为 `74cdf9e` 的部署（如果存在）
   - 或者找到最新的部署

4. **重新部署最新提交**：
   - 点击部署右侧的 **"..."** 菜单（三个点）
   - 选择 **"Redeploy"**
   - 或者直接点击 **"Redeploy"** 按钮
   - **重要**：确保选择的是最新提交，不是 `bb5869b`

### 方法 2：从项目设置触发部署

1. **进入项目页面**：
   - 在项目列表中点击 `vote1`

2. **查看最新提交**：
   - 在项目概览页面，应该能看到最新的 Git 提交
   - 确认显示的是 `74cdf9e` 或更新的提交

3. **手动触发部署**：
   - 如果显示的是旧提交，点击 **"Redeploy"** 按钮
   - 选择最新的提交（`74cdf9e`）

### 方法 3：清除缓存并重新部署

1. **清除构建缓存**：
   - Settings → General
   - 找到 "Clear Build Cache" 或类似选项
   - 清除缓存

2. **重新部署**：
   - 返回 Deployments 页面
   - 点击 "Redeploy"
   - 选择最新提交

## 📋 验证最新提交

### 检查 GitHub 仓库

访问 https://github.com/water4699/vote1，确认：
- 最新提交是 `74cdf9e` 或更新的
- `ui/package.json` 内容是最小化的（只有 name, version, private）

### 检查本地代码

```bash
git log --oneline -3
# 应该看到：
# 74cdf9e Add final pnpm fix documentation
# 8db3225 Create minimal ui/package.json to prevent Vercel pnpm detection
# 473bc1d Fix vercel.json and add old commit deployment fix guide

git show HEAD:ui/package.json
# 应该只包含：
# {
#   "name": "root",
#   "version": "0.4.0",
#   "private": true
# }
```

## ⚠️ 重要提示

**确保 Vercel 部署的是最新提交 `74cdf9e`，而不是 `bb5869b`。**

如果 Vercel 仍然部署 `bb5869b`：
1. 检查 Vercel 项目设置中的 Git 连接
2. 确认分支是 `main`
3. 手动触发重新部署并选择最新提交

## 🔄 部署后验证

部署成功后，检查构建日志应该看到：

1. **正确的提交**：
   ```
   Cloning github.com/water4699/vote1 (Branch: main, Commit: 74cdf9e)
   ```
   或更新的提交哈希

2. **没有 preinstall 错误**：
   - 不应该看到 `> root@0.4.0 preinstall`
   - 不应该看到 `> pnpm sdk:build`
   - 不应该看到 pnpm 版本错误

3. **使用 vercel.json 的 installCommand**：
   ```
   Running "cd packages/fhevm-sdk && npm install --legacy-peer-deps && npm run build && cd ../nextjs && npm install --legacy-peer-deps"
   ```

4. **构建成功**：
   - SDK 构建成功
   - Next.js 应用构建成功
   - 部署完成

## 📝 如果问题持续

如果 Vercel 仍然部署旧提交：

1. **检查 Git 连接**：
   - Settings → Git
   - 确认连接到 `water4699/vote1`
   - 确认分支是 `main`

2. **断开并重新连接 Git**：
   - Settings → Git
   - 断开连接
   - 重新连接到 `water4699/vote1`
   - 选择 `main` 分支

3. **联系支持**：
   - 如果问题持续，可能是 Vercel 的缓存问题
   - 可以联系 Vercel 支持

